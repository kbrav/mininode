import { expect as want } from 'chai';
import { Node } from '../node.js';

describe('node', () => {
  let ali, bob;
  beforeEach(async () => {
    ali = new Node([]);
    bob = new Node([]);
  });

  it('basic', async () => {});
});
